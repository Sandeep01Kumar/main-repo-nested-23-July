# Node.js dependency tree (transient install artifact; regenerated by `npm install`)
node_modules/

# npm lockfile — generated by `npm install`. Left untracked per setup policy;
# regenerated deterministically from package.json + the warmed npm cache.
package-lock.json

# Logs and misc npm noise
npm-debug.log*
.npm/
