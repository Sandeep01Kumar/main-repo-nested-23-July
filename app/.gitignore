# Node.js dependency tree (transient install artifact; regenerated by `npm install`)
node_modules/

# NOTE: package-lock.json is intentionally committed to version control (npm best
# practice) to pin the resolved dependency graph for reproducible installs via
# `npm ci`. It must NOT be ignored.

# Logs and misc npm noise
npm-debug.log*
.npm/
